# =============================================================================
# analysis_config.yaml — Single source of truth for all inference configs
# =============================================================================
#
# DB     : glue-dispenser-db (TimescaleDB)
# Table  : process_metrics
# Format : long → (timestamp, station_name, metric_name, value, unit, state_context)
#          DataLoader pivots long→wide (one row per cycle) before model pipelines
#
# Metric naming: <station>/<metric>
# target_column : the wide-format column name that is the prediction target
#
# analysis_types: forecast | anomaly | health_score | rul
# forecast_method: mamba
# anomaly_method:  zscore | western_electric | isolation_forest
# =============================================================================

# ALL_AVAILABLE_FEATURES:
# - system__count
# - shuttle_station__shuttle_pre_scan_time
# - shuttle_station__shuttle_scan_success_time
# - shuttle_station__shuttle_post_scan_delay
# - shuttle_station__shuttle_carrier_motion_time
# - shuttle_station__shuttle_stopper_lowering_time
# - shuttle_station__shuttle_pallet_release_time
# - shuttle_station__shuttle_stopper_rising_time
# - shuttle_station__shuttle_idle_time
# - system__cycle_time/
# - l1_buffer_a__buffer_holding_time
# - l1_buffer_a__buffer_idle_time
# - l1_buffer_a__buffer_pallet_release_time
# - l1_buffer_a__buffer_stopper_lowering_time
# - l1_buffer_a__buffer_stopper_rising_time
# - l1_buffer_b__buffer_holding_and_pre_stopper_down_time
# - l1_buffer_b__buffer_idle_time
# - l1_buffer_b__buffer_pallet_release_and_pre_stopper_up_time
# - l1_dispenser__machine_idle_time
# - l1_dispenser__dispenser_entry_unclamp_time
# - l1_dispenser__dispenser_delay_post_entry_unclamp
# - l1_dispenser__dispenser_pallet_lifting_1_time
# - l1_dispenser__dispenser_entry_clamping_time
# - l1_dispenser__dispenser_delay_post_clamping
# - l1_dispenser__dispenser_pallet_stopper_releasing_time
# - l1_dispenser__dispenser_delay_pre_gantry_positioning
# - l1_dispenser__dispenser_gantry_positioning_time
# - l1_dispenser__dispenser_dispensing_time
# - l1_dispenser__dispenser_pallet_unpositioning_time
# - l1_dispenser__dispenser_pallet_stopper_locking_time
# - l1_dispenser__dispenser_pre_exit_unclamping
# - l1_dispenser__dispenser_exit_unclamping
# - l1_dispenser__dispenser_post_exit_unclamp
# - l1_dispenser__dispenser_pallet_lowering_1
# - l1_dispenser__dispenser_stopper_lowering
# - l1_dispenser__dispenser_pallet_releasing
# - l1_dispenser__dispenser_stopper_rasing
# - l1_inspection__inspection_idle_time
# - l1_inspection__inspection_lifter_rising_time
# - l1_inspection__inspection_gantry_positioning_time
# - l1_inspection__inspection_core_vision_system_time
# - l1_inspection__inspection_post_vision_check_delay
# - l1_inspection__inspection_lifter_lowering_time
# - l1_inspection__inspection_pallet_releasing_time
# - l2_buffer_a__buffer_holding_and_pre_stopper_down_time
# - l2_buffer_a__buffer_idle_time
# - l2_buffer_a__buffer_pallet_release_and_pre_stopper_up_time
# - l2_buffer_b__buffer_holding_and_pre_stopper_down_time
# - l2_buffer_b__buffer_idle_time
# - l2_buffer_b__buffer_pallet_release_and_pre_stopper_up_time
# - l2_dispenser__machine_idle_time
# - l2_dispenser__dispenser_entry_unclamp_time
# - l2_dispenser__dispenser_delay_post_entry_unclamp
# - l2_dispenser__dispenser_pallet_lifting_1_time
# - l2_dispenser__dispenser_entry_clamping_time
# - l2_dispenser__dispenser_delay_post_clamping
# - l2_dispenser__dispenser_pallet_stopper_releasing_time
# - l2_dispenser__dispenser_delay_pre_gantry_positioning
# - l2_dispenser__dispenser_gantry_positioning_time
# - l2_dispenser__dispenser_dispensing_time
# - l2_dispenser__dispenser_pallet_unpositioning_time
# - l2_dispenser__dispenser_pallet_stopper_locking_time
# - l2_dispenser__dispenser_pre_exit_unclamping
# - l2_dispenser__dispenser_exit_unclamping
# - l2_dispenser__dispenser_post_exit_unclamp
# - l2_dispenser__dispenser_pallet_lowering_1
# - l2_dispenser__dispenser_stopper_lowering
# - l2_dispenser__dispenser_pallet_releasing
# - l2_dispenser__dispenser_stopper_rasing
# - l2_inspection__inspection_idle_time
# - l2_inspection__inspection_lifter_rising_time
# - l2_inspection__inspection_gantry_positioning_time
# - l2_inspection__inspection_core_vision_system_time
# - l2_inspection__inspection_post_vision_check_delay
# - l2_inspection__inspection_lifter_lowering_time
# - l2_inspection__inspection_pallet_releasing_time
# - timestamp

global:
  db:
    database_url_env: postgresql://postgres:postgres@localhost:5432/glue-dispenser-db
    input_table: process_metrics
    output_table: analysis_results
    schema: public
  mlflow:
    tracking_uri_env: MLFLOW_TRACKING_URI
    artifact_root_env: MLFLOW_ARTIFACT_ROOT

# ---------------------------------------------------------------------------
# Per-metric configs. Unset fields inherit from global.
# ---------------------------------------------------------------------------
target:
  # ── SYSTEM ────────────────────────────────────────────────────────────────
  system__cycle_time:
    #=================================================================================
    - method: mamba
      model_type: torch #changed to either torch or sklearn for ease
      save_name: mamba
      required_features:
        - system__cycle_time
        - timestamp

      history_window: 2
      stride: 1
      prediction_window: 1

      #TODO: make mlfow dockerized
      load_path: "saved_models/mamba/model.pt"

      arch:
        #place holders for mamba_ts class init not in arch already
        # TODO: Make input_size and seq_len for a single variable
        input_size: 1 #HACK: should do len(required_features) - 2 (because timestamp and target feature are in required_features)
        forecast_horizon: 1
        seq_len: 2
        hidden_dim: 32
        d_state: 16
        d_conv: 4
        num_layers: 3

        #HACK: these two should ideally be removed, as i feed tis into init for the model class
        # loss_weight_recon: 0.3
        # device: auto

      train_params:
        num_epochs: 150
        learning_rate: 0.002366405267469074
        dropout: 0.28334955521309035

        train_split: 0.7,
        val_split: 0.2,
        test_split: 0.1,

      analysis_types: [forecast]

    #=================================================================================
    - method: xgboost_forecast
      model_type: sklearn
      save_name: xgboost_forecast
      required_features:
        - system__cycle_time
        - timestamp

      load_path:
        "saved_models/xgboost_forecast/model.joblib"

        #INFO: this is for dataloader.
      history_window: 6
      stride: 2
      prediction_window: 4
      mode:
        offline

        #INFO: these are filled in xgboost class init
      arch:
        window_length: 6
        predict_horizon: 4
        n_estimators: 100
        max_depth: 3
        learning_rate: 0.1
        subsample: 0.8
        colsample_bytree: 0.8
        random_state: 42

      #HACK: right now train params arent used in the model class. need to add tem to the correct class train func
      train_params:
        n_estimators: 300
        max_depth: 6
        learning_rate: 0.05

        train_split: 0.7,
        val_split: 0.2,
        test_split: 0.1,
      analysis_types: [forecast]

    #=================================================================================
    - method: var_forecast
      model_type: sklearn
      save_name: var_forecast
      required_features:
        - system__cycle_time
        - timestamp # include other correlated features here for better VAR

      load_path: "saved_models/var_forecast/model.joblib"

      history_window: 10 # must be >= maxlags; this is your forecast anchor window
      stride: 1
      prediction_window: 1

      arch:
        maxlags: 5 # VAR lag order p; set to null for AIC auto-selection
        maxlags_search: 10 # only used when maxlags is null
        predict_horizon: 1
        target_col_idx: 0 # index of system__cycle_time in required_features (ex-timestamp)
        difference: false # set true if series is non-stationary

      analysis_types: [forecast]
  #=================================================================================

  # ── BUFFER ────────────────────────────────────────────────────────────────
  l1_buffer_a__health_score:
    #=================================================================================
    - method: isolation_forest
      save_name: l1_buffer_a__health_score
      model_type: sklearn
      required_features:
        #INFO: there are some extra in process logic sheet. i have only copied what was from mamba to make sure
        - l1_buffer_a__buffer_holding_time
        - l1_buffer_a__buffer_stopper_lowering_time
        - l1_buffer_a__buffer_pallet_release_time
        - l1_buffer_a__buffer_stopper_rising_time
        - timestamp

      load_path:
        "saved_models/l1_buffer_a__health_score/model.joblib"

        #INFO: this is for dataloader.
      history_window: 1
      stride: 1
      prediction_window: 1
      mode: offline

      #INFO: these are filled in health score class init
      arch:
        smoothing_window: 5 # was 20 — too large relative to dataset size
        n_estimators: 100
        contamination: "auto"
        drift_percentile: 5
        breakdown_sensitivity: 3.0
        random_state: 42

        #HACK: right now train params arent used in the model class. need to add tem to the correct class train func
      train_params:
        n_estimators: 300
        max_depth: 6
        learning_rate: 0.05

        train_split: 0.7,
        val_split: 0.2,
        test_split: 0.1,

      analysis_types: [score]
    #=================================================================================

  # ── BUFFER ────────────────────────────────────────────────────────────────
  l1_buffer_b__health_score:
    #=================================================================================
    - method: isolation_forest
      model_type: sklearn
      save_name: l1_buffer_b__health_score
      required_features:
        #INFO: there are some extra in process logic sheet. i have only copied what was from mamba to make sure
        - l1_buffer_b__buffer_holding_and_pre_stopper_down_time
        - wide-formatl1_buffer_b__buffer_idle_time
        - l1_buffer_b__buffer_pallet_release_and_pre_stopper_up_time
        - timestamp

        #INFO: this is for dataloader.

      history_window: 1
      stride: 1
      prediction_window: 1
      mode: offline

      load_path: "saved_models/l1_buffer_b__health_score/model.joblib"

      #INFO: these are filled in health score class init

      arch:
        smoothing_window: 5 # was 20 — too large relative to dataset size
        n_estimators: 100
        contamination: "auto"
        drift_percentile: 5
        breakdown_sensitivity: 3.0
        random_state: 42

        #HACK: right now train params arent used in the model class. need to add tem to the correct class train func
      train_params:
        n_estimators: 300
        max_depth: 6
        learning_rate: 0.05

        train_split: 0.7,
        val_split: 0.2,
        test_split: 0.1,

      analysis_types: [score]
    #=================================================================================

  # ── BUFFER ────────────────────────────────────────────────────────────────
  l2_buffer_a__health_score:
    - method: isolation_forest
      model_type: sklearn
      save_name: l2_buffer_a__health_score
      required_features:
        #INFO: there are some extra in process logic sheet. i have only copied what was from mamba to make sure
        - l2_buffer_a__buffer_holding_and_pre_stopper_down_time"
        - l2_buffer_a__buffer_idle_time
        - l2_buffer_a__buffer_pallet_release_and_pre_stopper_up_time
        - timestamp

        #INFO: this is for dataloader.

        #FIX: need to see if history window means anyting for this model
      history_window: 1
      stride: 1
      prediction_window: 1
      mode: offline

      load_path: "saved_models/l2_buffer_a__health_score/model.joblib"

      #INFO: these are filled in health score class init

      arch:
        smoothing_window: 5 # was 20 — too large relative to dataset size
        n_estimators: 100
        contamination: "auto"
        drift_percentile: 5
        breakdown_sensitivity: 3.0
        random_state: 42

        #HACK: right now train params arent used in the model class. need to add tem to the correct class train func
      train_params:
        n_estimators: 300
        max_depth: 6
        learning_rate: 0.05

        train_split: 0.7,
        val_split: 0.2,
        test_split: 0.1,

      analysis_types: [score]
    #=================================================================================

  l2_buffer_b__health_score:
    - method: isolation_forest
      model_type: sklearn
      save_name: l2_buffer_b__health_score
      required_features:
        #INFO: there are some extra in process logic sheet. i have only copied what was from mamba to make sure
        - l2_buffer_b__buffer_holding_and_pre_stopper_down_time"
        - l2_buffer_b__buffer_idle_time
        - l2_buffer_b__buffer_pallet_release_and_pre_stopper_up_time
        - timestamp

        #INFO: this is for dataloader.

        #FIX: need to see if history window means anyting for this model
      history_window: 1
      stride: 1
      prediction_window: 1
      mode: offline

      load_path: "saved_models/l2_buffer_b__health_score/model.joblib"

      #INFO: these are filled in health score class init

      arch:
        smoothing_window: 5 # was 20 — too large relative to dataset size
        n_estimators: 100
        contamination: "auto"
        drift_percentile: 5
        breakdown_sensitivity: 3.0
        random_state: 42

        #HACK: right now train params arent used in the model class. need to add tem to the correct class train func
      train_params:
        n_estimators: 300
        max_depth: 6
        learning_rate: 0.05

        train_split: 0.7,
        val_split: 0.2,
        test_split: 0.1,

      analysis_types: [score]
    #=================================================================================

  # ── SHUTTLE STATION ────────────────────────────────────────────────────────────────
  shuttle_station__health_score:
    #=================================================================================
    - method: isolation_forest
      model_type: sklearn
      save_name: shuttle_station__health_score
      required_features:
        - shuttle_station__shuttle_pre_scan_time
        - shuttle_station__shuttle_scan_success_time
        - shuttle_station__shuttle_post_scan_delay
        - shuttle_station__shuttle_carrier_motion_time
        - shuttle_station__shuttle_stopper_lowering_time
        - shuttle_station__shuttle_pallet_release_time
        - shuttle_station__shuttle_stopper_rising_time
        # - shuttle_station__shuttle_idle_time
        - timestamp

          #INFO: this is for dataloader.

          #FIX: need to see if history window means anyting for this model
      history_window: 1
      stride: 1
      prediction_window: 1
      mode: offline

      #INFO: these are filled in health score class init

      load_path: "saved_models/shuttle_station__health_score/model.joblib"

      arch:
        smoothing_window: 5 # was 20 — too large relative to dataset size
        n_estimators: 100
        contamination: "auto"
        drift_percentile: 5
        breakdown_sensitivity: 3.0
        random_state: 42

        #HACK: right now train params arent used in the model class. need to add tem to the correct class train func
      train_params:
        n_estimators: 300
        max_depth: 6
        learning_rate: 0.05

        train_split: 0.7,
        val_split: 0.2,
        test_split: 0.1,

      analysis_types: [score]
    #=================================================================================

  # ── DISPENSER ────────────────────────────────────────────────────────────────
  l1_dispenser__health_score:
    #=================================================================================
    - method: isolation_forest
      model_type: sklearn
      save_name: l1_dispenser_standard__health_score
      required_features:
        - l1_dispenser__dispenser_entry_unclamp_time
        - l1_dispenser__dispenser_delay_post_entry_unclamp
        - l1_dispenser__dispenser_pallet_lifting_1_time
        - l1_dispenser__dispenser_entry_clamping_time
        - l1_dispenser__dispenser_delay_post_clamping
        - l1_dispenser__dispenser_pallet_stopper_releasing_time
        - l1_dispenser__dispenser_delay_pre_gantry_positioning
        - l1_dispenser__dispenser_gantry_positioning_time
        - l1_dispenser__dispenser_dispensing_time
        - l1_dispenser__dispenser_pallet_unpositioning_time
        - l1_dispenser__dispenser_pallet_stopper_locking_time
        - l1_dispenser__dispenser_pre_exit_unclamping
        - l1_dispenser__dispenser_exit_unclamping
        - l1_dispenser__dispenser_post_exit_unclamp
        - l1_dispenser__dispenser_pallet_lowering_1
        - l1_dispenser__dispenser_stopper_lowering
        - l1_dispenser__dispenser_pallet_releasing
        - l1_dispenser__dispenser_stopper_rasing
        - timestamp

        #INFO: this is for dataloader.

      history_window: 1
      stride: 1
      prediction_window: 1
      mode: offline

      load_path: "saved_models/l1_dispenser_standard__health_score/model.joblib"

      #INFO: these are filled in health score class init

      arch:
        smoothing_window: 5 # was 20 — too large relative to dataset size
        n_estimators: 100
        contamination: "auto"
        drift_percentile: 5
        breakdown_sensitivity: 3.0
        random_state: 42

        #HACK: right now train params arent used in the model class. need to add tem to the correct class train func
      train_params:
        n_estimators: 300
        max_depth: 6
        learning_rate: 0.05

        train_split: 0.7,
        val_split: 0.2,
        test_split: 0.1,

      analysis_types: [score]
    #=================================================================================

  # ── DISPENSER ────────────────────────────────────────────────────────────────
  l2_dispenser__health_score:
    #=================================================================================
    - method: isolation_forest
      model_type: sklearn
      save_name: l2_dispenser_standard__health_score
      required_features:
        - l2_dispenser__dispenser_entry_unclamp_time
        - l2_dispenser__dispenser_delay_post_entry_unclamp
        - l2_dispenser__dispenser_pallet_lifting_1_time
        - l2_dispenser__dispenser_entry_clamping_time
        - l2_dispenser__dispenser_delay_post_clamping
        - l2_dispenser__dispenser_pallet_stopper_releasing_time
        - l2_dispenser__dispenser_delay_pre_gantry_positioning
        - l2_dispenser__dispenser_gantry_positioning_time
        - l2_dispenser__dispenser_dispensing_time
        - l2_dispenser__dispenser_pallet_unpositioning_time
        - l2_dispenser__dispenser_pallet_stopper_locking_time
        - l2_dispenser__dispenser_pre_exit_unclamping
        - l2_dispenser__dispenser_exit_unclamping
        - l2_dispenser__dispenser_post_exit_unclamp
        - l2_dispenser__dispenser_pallet_lowering_1
        - l2_dispenser__dispenser_stopper_lowering
        - l2_dispenser__dispenser_pallet_releasing
        - l2_dispenser__dispenser_stopper_rasing
        - timestamp

        #INFO: this is for dataloader.

        #FIX: need to see if history window means anyting for this model
      history_window: 1
      stride: 1
      prediction_window: 1
      mode: offline

      load_path: "saved_models/l2_dispenser_standard__health_score/model.joblib"

      #INFO: these are filled in health score class init

      arch:
        smoothing_window: 5 # was 20 — too large relative to dataset size
        n_estimators: 100
        contamination: "auto"
        drift_percentile: 5
        breakdown_sensitivity: 3.0
        random_state: 42

        #HACK: right now train params arent used in the model class. need to add tem to the correct class train func
      train_params:
        n_estimators: 300
        max_depth: 6
        learning_rate: 0.05

        train_split: 0.7,
        val_split: 0.2,
        test_split: 0.1,

      analysis_types: [score]
    #=================================================================================

  # ── INSPECTION ────────────────────────────────────────────────────────────────
  l1_inspection__health_score:
    #=================================================================================
    - method: isolation_forest
      model_type: sklearn
      save_name: l1_inspection_standard__health_score
      required_features:
        - l1_inspection__inspection_lifter_rising_time
        - l1_inspection__inspection_gantry_positioning_time
        - l1_inspection__inspection_core_vision_system_time
        - l1_inspection__inspection_post_vision_check_delay
        - l1_inspection__inspection_lifter_lowering_time
        - l1_inspection__inspection_pallet_releasing_time
        - timestamp

        #INFO: this is for dataloader.

        #FIX: need to see if history window means anyting for this model
      history_window: 1
      stride: 1
      prediction_window: 1
      mode: offline

      load_path: "saved_models/l1_inspection_standard__health_score/model.joblib"

      #INFO: these are filled in health score class init

      arch:
        smoothing_window: 5 # was 20 — too large relative to dataset size
        n_estimators: 100
        contamination: "auto"
        drift_percentile: 5
        breakdown_sensitivity: 3.0
        random_state: 42

        #HACK: right now train params arent used in the model class. need to add tem to the correct class train func
      train_params:
        n_estimators: 300
        max_depth: 6
        learning_rate: 0.05

        train_split: 0.7,
        val_split: 0.2,
        test_split: 0.1,

      analysis_types: [score]
    #=================================================================================

  # ── INSPECTION ────────────────────────────────────────────────────────────────
  l2_inspection__health_score:
    #=================================================================================
    - method: isolation_forest
      model_type: sklearn
      save_name: l2_inspection_standard__health_score
      required_features:
        - l2_inspection__inspection_lifter_rising_time
        - l2_inspection__inspection_gantry_positioning_time
        - l2_inspection__inspection_core_vision_system_time
        - l2_inspection__inspection_post_vision_check_delay
        - l2_inspection__inspection_lifter_lowering_time
        - l2_inspection__inspection_pallet_releasing_time
        - timestamp

        #INFO: this is for dataloader.

        #FIX: need to see if history window means anyting for this model
      history_window: 1
      stride: 1
      prediction_window: 1
      mode: offline

      #INFO: these are filled in health score class init

      load_path: "saved_models/l2_inspection_standard__health_score/model.joblib"

      arch:
        smoothing_window: 5 # was 20 — too large relative to dataset size
        n_estimators: 100
        contamination: "auto"
        drift_percentile: 5
        breakdown_sensitivity: 3.0
        random_state: 42

        #HACK: right now train params arent used in the model class. need to add tem to the correct class train func
      train_params:
        n_estimators: 300
        max_depth: 6
        learning_rate: 0.05

        train_split: 0.7,
        val_split: 0.2,
        test_split: 0.1,

      analysis_types: [score]
    #=================================================================================
